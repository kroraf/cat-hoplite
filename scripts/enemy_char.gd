extends Unit

#func _ready():
	#if is_enemy:
		#var player = get_tree().get_first_node_in_group("player")
		#ai_controller.initialize(self, player)
#
#func take_turn() -> void:
	#ai_controller.take_turn()
